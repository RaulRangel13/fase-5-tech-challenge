apiVersion: apps/v1
kind: Deployment
metadata:
  name: agro-identity
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agro-identity
  template:
    metadata:
      labels:
        app: agro-identity
    spec:
      containers:
        - name: identity
          image: agro-identity:latest
          imagePullPolicy: Never
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Development"
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: agro-secrets
                  key: CONNECTION_STRING_IDENTITY
            - name: JwtSettings__SecretKey
              valueFrom:
                secretKeyRef:
                  name: agro-secrets
                  key: JWT_SECRET_KEY
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: agro-identity
  namespace: default
spec:
  type: NodePort
  selector:
    app: agro-identity
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30001
